---
repo: open-telemetry/opentelemetry-collector
path: receiver/otlpreceiver
version: v0.155.0
scope_name: go.opentelemetry.io/collector/receiver/otlpreceiver
commit_sha: 41da2fbd7dd58a5dd299587ebb159025b28521d2
last_verified: 2026-08-11
---

# OTLP Receiver

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `otlp/TraceDataReceived` | Internal | `transport`; `format`; `accepted_spans`; `refused_spans`; `failed_spans` | `transport` is the configured transport; `format` is `protobuf`; count attributes record the outcome. `refused_spans` and `failed_spans` semantics are controlled by feature gate `receiverhelper.newReceiverMetrics`. |
| `otlp/MetricsReceived` | Internal | `transport`; `format`; `accepted_metric_points`; `refused_metric_points`; `failed_metric_points` | `format` is `protobuf`; count attributes record the outcome. `refused_metric_points` and `failed_metric_points` semantics are controlled by feature gate `receiverhelper.newReceiverMetrics`. |
| `otlp/LogsReceived` | Internal | `transport`; `format`; `accepted_log_records`; `refused_log_records`; `failed_log_records` | `format` is `protobuf`; count attributes record the outcome. `refused_log_records` and `failed_log_records` semantics are controlled by feature gate `receiverhelper.newReceiverMetrics`. |
| `otlp/ProfilesReceived` | Internal | `transport`; `format`; `accepted_profile_samples`; `refused_profile_samples`; `failed_profile_samples` | Profiles are alpha stability. `format` is `protobuf`; count attributes record the outcome. The refused/failed semantics are controlled by feature gate `receiverhelper.newReceiverMetrics`. |

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `otelcol_receiver_accepted_spans` | Counter | `{span}` | `receiver`, `transport` | - |
| `otelcol_receiver_refused_spans` | Counter | `{span}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics` distinguishes downstream refusal from internal failure; when disabled, errors are counted as refused. |
| `otelcol_receiver_failed_spans` | Counter | `{span}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics`; records internal failures when enabled, otherwise receives zero for errors. |
| `otelcol_receiver_accepted_metric_points` | Counter | `{datapoint}` | `receiver`, `transport` | - |
| `otelcol_receiver_refused_metric_points` | Counter | `{datapoint}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics` distinguishes downstream refusal from internal failure; when disabled, errors are counted as refused. |
| `otelcol_receiver_failed_metric_points` | Counter | `{datapoint}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics`; records internal failures when enabled, otherwise receives zero for errors. |
| `otelcol_receiver_accepted_log_records` | Counter | `{record}` | `receiver`, `transport` | - |
| `otelcol_receiver_refused_log_records` | Counter | `{record}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics` distinguishes downstream refusal from internal failure; when disabled, errors are counted as refused. |
| `otelcol_receiver_failed_log_records` | Counter | `{record}` | `receiver`, `transport` | `receiverhelper.newReceiverMetrics`; records internal failures when enabled, otherwise receives zero for errors. |
| `otelcol_receiver_accepted_profile_samples` | Counter | `{sample}` | `receiver`, `transport` | Profiles are alpha stability. |
| `otelcol_receiver_refused_profile_samples` | Counter | `{sample}` | `receiver`, `transport` | Profiles are alpha stability. `receiverhelper.newReceiverMetrics` distinguishes downstream refusal from internal failure; when disabled, errors are counted as refused. |
| `otelcol_receiver_failed_profile_samples` | Counter | `{sample}` | `receiver`, `transport` | Profiles are alpha stability. `receiverhelper.newReceiverMetrics`; records internal failures when enabled, otherwise receives zero for errors. |
| `otelcol_receiver_requests` | Counter | `{request}` | `receiver`, `transport`, `outcome` (`success`, `refused`, or `failure`) | Emitted only when feature gate `receiverhelper.newReceiverMetrics` is enabled. |

## Sources

- `receiver/otlpreceiver/internal/trace/otlp.go`
- `receiver/otlpreceiver/internal/metrics/otlp.go`
- `receiver/otlpreceiver/internal/logs/otlp.go`
- `receiver/otlpreceiver/internal/profiles/otlp.go`
- `receiver/otlpreceiver/internal/metadata/generated_status.go`
- `receiver/receiverhelper/obsreport.go`
- `receiver/receiverhelper/internal/metadata/generated_telemetry.go`
- `receiver/receiverhelper/internal/obsmetrics.go`
- `receiver/receiverhelper/internal/metadata/generated_feature_gates.go`
