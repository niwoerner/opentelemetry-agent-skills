---
repo: open-telemetry/opentelemetry-collector
path: exporter/debugexporter
version: v0.155.0
scope_name: go.opentelemetry.io/collector/exporter/debugexporter
commit_sha: 41da2fbd7dd58a5dd299587ebb159025b28521d2
last_verified: 2026-08-11
---

# Debug Exporter

## Logs

| Log/event name | Attributes | Notes |
|---|---|---|
| `Traces` | `resource spans`; `spans` | INFO summary emitted for each received trace batch. With `verbosity: normal` or `detailed`, an additional INFO message contains the marshaled input; output is subject to `sampling_initial` and `sampling_thereafter`. |
| `Metrics` | `resource metrics`; `metrics`; `data points` | INFO summary emitted for each received metrics batch. With `verbosity: normal` or `detailed`, an additional INFO message contains the marshaled input; output is subject to `sampling_initial` and `sampling_thereafter`. |
| `Logs` | `resource logs`; `log records` | INFO summary emitted for each received logs batch. With `verbosity: normal` or `detailed`, an additional INFO message contains the marshaled input; output is subject to `sampling_initial` and `sampling_thereafter`. |
| `Profiles` | `resource profiles`; `sample records` | INFO summary emitted for each received profiles batch. With `verbosity: normal` or `detailed`, an additional INFO message contains the marshaled input; output is subject to `sampling_initial` and `sampling_thereafter`. |

## Sources

- `exporter/debugexporter/exporter.go`
- `exporter/debugexporter/factory.go`
- `exporter/debugexporter/config.go`
- `exporter/debugexporter/README.md`
- `exporter/debugexporter/metadata.yaml`
