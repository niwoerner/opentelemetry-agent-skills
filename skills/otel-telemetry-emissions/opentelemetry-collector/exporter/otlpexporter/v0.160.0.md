---
repo: open-telemetry/opentelemetry-collector
path: exporter/otlpexporter
version: v0.160.0
scope_name: go.opentelemetry.io/collector/exporter/otlpexporter
commit_sha: cd3455cf3a7f672208140b1ebb1581c542b2b0ed
last_verified: 2026-09-03
---

# OTLP exporter

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `exporter/<exporter ID>/<signal>` | INTERNAL | `exporter=<exporter ID>`; `data_type=<traces\|metrics\|logs\|profiles>` | Created by exporterhelper for each export request; includes queue links when queued. Sets `items.sent` and `items.failed`; errors set span status to ERROR. |
| `exporter/enqueue` | INTERNAL | - | Created when a sending queue is configured, for each queue offer. |

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `otelcol_exporter_enqueue_failed_log_records` | Counter | `{record}` | `exporter` | Queue configured; emitted when log records cannot be enqueued. |
| `otelcol_exporter_enqueue_failed_metric_points` | Counter | `{datapoint}` | `exporter` | Queue configured; emitted when metric points cannot be enqueued. |
| `otelcol_exporter_enqueue_failed_profile_samples` | Counter | `{sample}` | `exporter` | Queue configured; emitted when profile samples cannot be enqueued. |
| `otelcol_exporter_enqueue_failed_spans` | Counter | `{span}` | `exporter` | Queue configured; emitted when spans cannot be enqueued. |
| `otelcol_exporter_enqueue_size` | Histogram | `{unit}` | `exporter` | Queue configured; emitted at detailed telemetry level for each offered request. |
| `otelcol_exporter_enqueue_size_bytes` | Histogram | `By` | `exporter` | Queue configured; emitted at detailed telemetry level for each offered request. |
| `otelcol_exporter_in_flight_requests` | UpDownCounter | `{request}` | `exporter`; `data_type` | Includes requests in retry backoff. |
| `otelcol_exporter_queue_batch_send_size` | Histogram | `{unit}` | `exporter` | Queue configured and batching enabled; emitted at detailed telemetry level. The default queue batching behavior is controlled by feature gate `pkg.exporterhelper.queueBatchEnabled`. |
| `otelcol_exporter_queue_batch_send_size_bytes` | Histogram | `By` | `exporter` | Queue configured and batching enabled; emitted at detailed telemetry level. The default queue batching behavior is controlled by feature gate `pkg.exporterhelper.queueBatchEnabled`. |
| `otelcol_exporter_queue_capacity` | Gauge | `{batch}` | `exporter`; `data_type` | Queue configured; asynchronous fixed retry-queue capacity. |
| `otelcol_exporter_queue_size` | Gauge | `{batch}` | `exporter`; `data_type` | Queue configured; asynchronous current retry-queue size. |
| `otelcol_exporter_send_failed_log_records` | Counter | `{record}` | `exporter`; `server.address`; `server.port`; `error.type`; `error.permanent` | Emitted for failed sends; `error.type` and `error.permanent` are included at detailed telemetry level. |
| `otelcol_exporter_send_failed_metric_points` | Counter | `{datapoint}` | `exporter`; `server.address`; `server.port`; `error.type`; `error.permanent` | Emitted for failed sends; `error.type` and `error.permanent` are included at detailed telemetry level. |
| `otelcol_exporter_send_failed_profile_samples` | Counter | `{sample}` | `exporter`; `server.address`; `server.port`; `error.type`; `error.permanent` | Emitted for failed sends; `error.type` and `error.permanent` are included at detailed telemetry level. |
| `otelcol_exporter_send_failed_spans` | Counter | `{span}` | `exporter`; `server.address`; `server.port`; `error.type`; `error.permanent` | Emitted for failed sends; `error.type` and `error.permanent` are included at detailed telemetry level. |
| `otelcol_exporter_sent_log_records` | Counter | `{record}` | `exporter`; `server.address`; `server.port` | Emitted for successfully sent log records. |
| `otelcol_exporter_sent_metric_points` | Counter | `{datapoint}` | `exporter`; `server.address`; `server.port` | Emitted for successfully sent metric points. |
| `otelcol_exporter_sent_profile_samples` | Counter | `{sample}` | `exporter`; `server.address`; `server.port` | Emitted for successfully sent profile samples. |
| `otelcol_exporter_sent_spans` | Counter | `{span}` | `exporter`; `server.address`; `server.port` | Emitted for successfully sent spans. |

## Logs

| Log/event name | Attributes | Notes |
|---|---|---|
| `Partial success response` | `message`; one of `dropped_spans`, `dropped_data_points`, `dropped_log_records`, or `dropped_profiles` | Emitted at WARN level when the OTLP response reports an error message or rejected items. |

## Sources

- `exporter/otlpexporter/factory.go`
- `exporter/otlpexporter/otlp.go`
- `exporter/otlpexporter/internal/metadata/generated_status.go`
- `exporter/exporterhelper/metadata.yaml`
- `exporter/exporterhelper/internal/metadata/generated_telemetry.go`
- `exporter/exporterhelper/internal/metadata/generated_feature_gates.go`
- `exporter/exporterhelper/internal/obs_report_sender.go`
- `exporter/exporterhelper/internal/queue/obs_queue.go`
