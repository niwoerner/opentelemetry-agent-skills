---
repo: open-telemetry/opentelemetry-js-contrib
path: packages/instrumentation-ioredis
version: v0.68.0
scope_name: "@opentelemetry/instrumentation-ioredis"
commit_sha: 8d7daea5c404855f474a82f4296640af8b93b64c
last_verified: 2026-08-06
---

# instrumentation-ioredis

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `connect` | CLIENT | `db.system.name="redis"`, `db.query.text="connect"`; `server.address=<host>`, `server.port=<port>`; `db.system="redis"`, `db.statement="connect"`, `db.connection_string="redis://<host>:<port>"`; `net.peer.name=<host>`, `net.peer.port=<port>` | Emitted only when an active parent span exists unless `requireParentSpan: false`. Stable database attributes are gated by the `database` mode of `OTEL_SEMCONV_STABILITY_OPT_IN`; legacy database attributes by its `database/old` mode, and stable network attributes by its `http` mode (legacy network attributes by `http/old`). On failure, records an exception event and sets span status to ERROR. |
| `<redis command name>` | CLIENT | `db.system.name="redis"`, `db.query.text=<serialized command>`; `server.address=<host>`, `server.port=<port>`; `db.system="redis"`, `db.statement=<serialized command>`, `db.connection_string="redis://<host>:<port>"`; `net.peer.name=<host>`, `net.peer.port=<port>` | Emitted only when an active parent span exists unless `requireParentSpan: false`. The query value uses `dbStatementSerializer` when configured, otherwise the default serializer. Stable database attributes are gated by the `database` mode of `OTEL_SEMCONV_STABILITY_OPT_IN`; legacy database attributes by its `database/old` mode, and stable network attributes by its `http` mode (legacy network attributes by `http/old`). On failure, records an exception event and sets span status to ERROR; `requestHook` and `responseHook` may add custom attributes. |

## Sources

- `packages/instrumentation-ioredis/src/instrumentation.ts`
- `packages/instrumentation-ioredis/src/semconv.ts`
- `packages/instrumentation-ioredis/src/utils.ts`
- `packages/instrumentation-ioredis/src/types.ts`
- `packages/instrumentation-ioredis/package.json`
