---
repo: open-telemetry/opentelemetry-js-contrib
path: packages/instrumentation-ioredis
version: v0.66.0
scope_name: "@opentelemetry/instrumentation-ioredis"
commit_sha: 15ef7506553f631ea4181391e0c5725a56f0d082
last_verified: 2026-08-11
---

# instrumentation-ioredis

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `connect` | CLIENT | `db.system.name="redis"`, `db.query.text="connect"`; `server.address=<host>`, `server.port=<port>`; `db.system="redis"`, `db.statement="connect"`, `db.connection_string="redis://<host>:<port>"`; `net.peer.name=<host>`, `net.peer.port=<port>` | Emitted only when an active parent span exists unless `requireParentSpan: false`. Stable database attributes are enabled by the `database` mode of `OTEL_SEMCONV_STABILITY_OPT_IN`; legacy database attributes are emitted by default and with `database/dup`. Stable network attributes are enabled by the `http` mode; legacy network attributes are emitted by default and with `http/dup`. On failure, records an exception event and sets span status to ERROR. |
| `<redis command name>` | CLIENT | `db.system.name="redis"`, `db.query.text=<serialized command>`; `server.address=<host>`, `server.port=<port>`; `db.system="redis"`, `db.statement=<serialized command>`, `db.connection_string="redis://<host>:<port>"`; `net.peer.name=<host>`, `net.peer.port=<port>` | Emitted only when an active parent span exists unless `requireParentSpan: false`. The query value uses `dbStatementSerializer` when configured, otherwise the default serializer. Stable database attributes are enabled by the `database` mode of `OTEL_SEMCONV_STABILITY_OPT_IN`; legacy database attributes are emitted by default and with `database/dup`. Stable network attributes are enabled by the `http` mode; legacy network attributes are emitted by default and with `http/dup`. On failure, records an exception event and sets span status to ERROR; `requestHook` and `responseHook` may add custom attributes. |

## Sources

- `packages/instrumentation-ioredis/src/instrumentation.ts`
- `packages/instrumentation-ioredis/src/semconv.ts`
- `packages/instrumentation-ioredis/src/utils.ts`
- `packages/instrumentation-ioredis/src/types.ts`
- `packages/instrumentation-ioredis/package.json`
- `packages/instrumentation-ioredis/README.md`
