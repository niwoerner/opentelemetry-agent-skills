---
repo: open-telemetry/opentelemetry-java-instrumentation
path: instrumentation/servlet
version: v2.29.0
scope_name: io.opentelemetry.servlet-2.2, io.opentelemetry.servlet-3.0, io.opentelemetry.servlet-5.0
commit_sha: c215cb489a90d95c2ea5808fbdcd2f7d81d2f2ae
last_verified: 2026-08-11
---

# Servlet

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `<HTTP method> [<route>]` | SERVER | `client.address`, `error.type`, `http.request.method`, `http.request.method_original`, `http.response.status_code`, `http.route` (Servlet 3+/5 only), `network.peer.address`, `network.peer.port` (Servlet 3+/5 only), `network.protocol.name`, `network.protocol.version`, `server.address`, `server.port`, `url.path`, `url.query`, `url.scheme`, `user_agent.original`; optionally `servlet.request.parameter.<name>` (string array), `servlet.timeout`, `user.name`/`enduser.id` | Parameter capture, experimental span attributes, and agent user-name capture are opt-in. `user.name` is used with semconv v3 preview; otherwise `enduser.id`. |
| `HttpServletResponse.sendRedirect` | INTERNAL | `code.function`, `code.namespace` | Java agent response instrumentation only; active server span required. |
| `HttpServletResponse.sendError` | INTERNAL | `code.function`, `code.namespace` | Java agent response instrumentation only; active server span required. |

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `http.server.request.duration` | Histogram | `s` | `error.type`, `http.request.method`, `http.response.status_code`, `http.route` (Servlet 3+/5 only), `network.protocol.name`, `network.protocol.version`, `server.address`, `server.port`, `url.scheme` | - |
| `http.server.active_requests` | UpDownCounter | `{requests}` | HTTP server request attributes available at start | Requires experimental HTTP server telemetry. |
| `http.server.request.body.size` | Histogram | `By` | HTTP server request attributes | Requires experimental HTTP server telemetry; only recorded when size is available. |
| `http.server.response.body.size` | Histogram | `By` | HTTP server response attributes | Requires experimental HTTP server telemetry; only recorded when size is available. |

## Sources

- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletHttpAttributesGetter.java`
- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletAdditionalAttributesExtractor.java`
- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletRequestParametersExtractor.java`
- `instrumentation/servlet/servlet-common/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/common/response/ResponseInstrumenterFactory.java`
- `instrumentation/servlet/servlet-{2.2,3.0,5.0}/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/*/Servlet*Singletons.java`
