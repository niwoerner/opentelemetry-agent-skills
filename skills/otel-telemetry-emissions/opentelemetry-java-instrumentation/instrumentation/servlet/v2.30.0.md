---
repo: open-telemetry/opentelemetry-java-instrumentation
path: instrumentation/servlet
version: v2.30.0
scope_name: io.opentelemetry.servlet-2.2, io.opentelemetry.servlet-3.0, io.opentelemetry.servlet-5.0
commit_sha: 080c1d32ca676ba7b0210c21b60cce8e7ac39058
last_verified: 2026-08-11
---

# Servlet

## Traces

| Span name | Kind | Attributes | Notes |
|---|---|---|---|
| `<HTTP method> [<route>]` | SERVER | `client.address`, `error.type`, `http.request.method`, `http.request.method_original`, `http.response.status_code`, `http.route` (Servlet 3+/5 only), `network.peer.address`, `network.peer.port` (Servlet 3+/5 only), `network.protocol.name`, `network.protocol.version`, `server.address`, `server.port`, `url.path`, `url.query`, `url.scheme`, `user_agent.original`; optionally `servlet.request.parameter.<name>` (string array), `servlet.timeout`, `user.name`/`enduser.id`, `http.request.body.size`, `http.response.body.size` | `servlet.request.parameter.<name>` requires configured request-parameter capture; `servlet.timeout` requires `otel.instrumentation.servlet.experimental-span-attributes`; `user.name` (semconv v3 preview) or `enduser.id` requires agent user-name capture; body-size attributes require experimental HTTP server telemetry. |
| `HttpServletResponse.sendRedirect` | INTERNAL | `code.function`, `code.namespace` | Java agent response instrumentation only; emitted when called inside an active servlet server span. |
| `HttpServletResponse.sendError` | INTERNAL | `code.function`, `code.namespace` | Java agent response instrumentation only; emitted when called inside an active servlet server span. |

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `http.server.request.duration` | Histogram | `s` | `error.type`, `http.request.method`, `http.response.status_code`, `http.route` (Servlet 3+/5 only), `network.protocol.name`, `network.protocol.version`, `server.address`, `server.port`, `url.scheme` | - |
| `http.server.active_requests` | UpDownCounter | `{requests}` | HTTP server request attributes available at start, including `http.request.method`, `server.address`, `server.port`, `url.scheme`, `network.protocol.name`, `network.protocol.version` | Requires experimental HTTP server telemetry (`http.server.emit-experimental-telemetry`). |
| `http.server.request.body.size` | Histogram | `By` | HTTP server request attributes | Requires experimental HTTP server telemetry; recorded only when a request body size is available. |
| `http.server.response.body.size` | Histogram | `By` | HTTP server response attributes | Requires experimental HTTP server telemetry; recorded only when a response body size is available. |

## Sources

- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletHttpAttributesGetter.java`
- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletAdditionalAttributesExtractor.java`
- `instrumentation/servlet/servlet-common/library/src/main/java/io/opentelemetry/instrumentation/servlet/common/internal/ServletRequestParametersExtractor.java`
- `instrumentation/servlet/servlet-common/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/common/response/ResponseInstrumenterFactory.java`
- `instrumentation/servlet/servlet-2.2/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/v2_2/Servlet2Singletons.java`
- `instrumentation/servlet/servlet-3.0/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/v3_0/Servlet3Singletons.java`
- `instrumentation/servlet/servlet-5.0/javaagent/src/main/java/io/opentelemetry/javaagent/instrumentation/servlet/v5_0/Servlet5Singletons.java`
