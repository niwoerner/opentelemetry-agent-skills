---
repo: open-telemetry/opentelemetry-collector-contrib
path: receiver/k8seventsreceiver
version: v0.159.0
scope_name: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/k8seventsreceiver
commit_sha: 1120cc0714bf4d14600105e5673c3b00fe87467a
last_verified: 2026-08-24
---

# Kubernetes Events Receiver

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `otelcol_k8s_events_modified_filtered` | Counter | `{events}` | - | Counts MODIFIED watch events filtered by `dedup_interval`; always 0 when `dedup_interval` is not set. |

## Logs

| Log/event name | Attributes | Notes |
|---|---|---|
| `k8s.event` | `k8s.event.reason`, `k8s.event.action`, `k8s.event.start_time`, `k8s.event.name`, `k8s.event.uid`, `k8s.namespace.name`, `k8s.event.reporting_controller`, `k8s.event.reporting_instance`, `k8s.event.count` (when nonzero); resource attributes `k8s.node.name`, `k8s.object.kind`, `k8s.object.name`, `k8s.object.uid`, `k8s.object.fieldpath`, `k8s.object.api_version`, `k8s.object.resource_version` | Emits accepted ADDED events and MODIFIED events allowed by `dedup_interval`; DELETED events and events older than receiver startup are excluded. `k8s.event.count` is omitted when zero. Severity is mapped from event type `Normal`, `Warning`, `Error`, or `Critical`. |

## Sources

- `receiver/k8seventsreceiver/k8s_event_to_logdata.go`
- `receiver/k8seventsreceiver/receiver.go`
- `receiver/k8seventsreceiver/metadata.yaml`
- `receiver/k8seventsreceiver/documentation.md`
- `receiver/k8seventsreceiver/internal/metadata/generated_telemetry.go`
- `receiver/k8seventsreceiver/internal/metadata/generated_status.go`
