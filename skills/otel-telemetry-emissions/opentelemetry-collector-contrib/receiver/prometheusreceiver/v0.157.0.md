---
repo: open-telemetry/opentelemetry-collector-contrib
path: receiver/prometheusreceiver
version: v0.157.0
scope_name: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/prometheusreceiver
commit_sha: 89e43555904cd97c2d36605347c5d5237b1bdc8c
last_verified: 2026-08-11
---

# Prometheus Receiver

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `<Prometheus gauge family>` | Gauge | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Any scraped gauge or unknown-typed family; metric name, description, and unit come from scrape metadata. |
| `<Prometheus counter family>` | Sum (cumulative, monotonic) | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Scraped counter families; created timestamps are used when present. Zero-valued created samples require feature gate `receiver.prometheusreceiver.EnableCreatedTimestampZeroIngestion`. |
| `<Prometheus info or stateset family>` | Sum (cumulative, non-monotonic) | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Prometheus info and stateset types are represented as non-monotonic sums. |
| `<Prometheus classic histogram family>` | Histogram (cumulative) | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, `le`, and `otel_scope_*` | Bucket, sum, and count series are combined. |
| `<Prometheus native histogram family>` | Exponential Histogram (cumulative) | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Native histograms are converted to OTLP exponential histograms. |
| `<Prometheus native histogram custom-bucket family>` | Histogram (cumulative) | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Native custom-bucket schema (`-53`) is converted to an OTLP histogram. |
| `<Prometheus summary family>` | Summary | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, `quantile`, and `otel_scope_*` | Quantile, sum, and count series are combined. |
| `up` | Gauge | `1` | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Built-in scrape health metric. |
| `scrape_duration_seconds` | Gauge | `s` | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Built-in scrape metric. |
| `scrape_samples_scraped` | Gauge | `1` | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Built-in scrape metric. |
| `scrape_series_added` | Gauge | `1` | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Built-in scrape metric. |
| `scrape_samples_post_metric_relabeling` | Gauge | `1` | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Built-in scrape metric. |
| `otel_scope_info` | Gauge | Prometheus metadata unit, converted to UCUM | Prometheus labels except `__name__`, `job`, `instance`, `scheme`, `metrics_path`, and `otel_scope_*` | Emitted only with feature gate `receiver.prometheusreceiver.IgnoreScopeInfoMetric`; otherwise converted to scope data. |

## Sources

- `receiver/prometheusreceiver/metadata.yaml`
- `receiver/prometheusreceiver/internal/metadata.go`
- `receiver/prometheusreceiver/internal/metricfamily.go`
- `receiver/prometheusreceiver/internal/prom_to_otlp.go`
- `receiver/prometheusreceiver/internal/transaction.go`
- `receiver/prometheusreceiver/internal/util.go`
- `receiver/prometheusreceiver/metrics_receiver.go`
