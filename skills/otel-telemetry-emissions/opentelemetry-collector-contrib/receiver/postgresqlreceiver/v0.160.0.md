---
repo: open-telemetry/opentelemetry-collector-contrib
path: receiver/postgresqlreceiver
version: v0.160.0
scope_name: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/postgresqlreceiver
commit_sha: 982f20b8a8e8a2569fab3e27cf8b008e8a5080c1
last_verified: 2026-09-03
---

# PostgreSQL Receiver
## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `postgresql.backends` | Sum | 1 | db.namespace | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.bgwriter.buffers.allocated` | Sum | {buffers} | - | - |
| `postgresql.bgwriter.buffers.writes` | Sum | {buffers} | source | - |
| `postgresql.bgwriter.checkpoint.count` | Sum | {checkpoints} | type | - |
| `postgresql.bgwriter.duration` | Sum | ms | type | - |
| `postgresql.bgwriter.maxwritten` | Sum | 1 | - | - |
| `postgresql.blks_hit` | Sum | {blks_hit} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.blks_read` | Sum | {blks_read} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.blocks_read` | Sum | 1 | source, db.namespace, db.collection.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.commits` | Sum | 1 | db.namespace | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.connection.max` | Gauge | {connections} | - | - |
| `postgresql.database.count` | Sum | {databases} | - | - |
| `postgresql.database.locks` | Gauge | {lock} | relation, mode, lock_type, db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.db_size` | Sum | By | db.namespace | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.deadlocks` | Sum | {deadlock} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.function.calls` | Sum | {call} | function, db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires track_functions=pl|all. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.index.scans` | Sum | {scans} | db.namespace, db.collection.name, postgresql.index.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.index.size` | Gauge | By | db.namespace, db.collection.name, postgresql.index.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.operations` | Sum | 1 | operation, db.namespace, db.collection.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.query.conflicts` | Sum | {query} | postgresql.conflict.type, db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires standby recovery statistics; otherwise may be zero. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.query.execution.time` | Sum | s | db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.replication.data_delay` | Gauge | By | replication_client | Requires WAL and at least one replica. |
| `postgresql.rollbacks` | Sum | 1 | db.namespace | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.rows` | Sum | 1 | state, db.namespace, db.collection.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.sequential_scans` | Sum | {sequential_scan} | db.namespace, db.collection.name | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.table.count` | Sum | {table} | db.namespace | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.table.size` | Sum | By | db.namespace, db.collection.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.table.vacuum.count` | Sum | {vacuum} | db.namespace, db.collection.name | Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.temp.io` | Sum | By | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.temp_files` | Sum | {temp_file} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.tup_deleted` | Sum | {tup_deleted} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.tup_fetched` | Sum | {tup_fetched} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.tup_inserted` | Sum | {tup_inserted} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.tup_returned` | Sum | {tup_returned} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.tup_updated` | Sum | {tup_updated} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.vector.insert.duration` | Sum | s | db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Requires pgvector and pg_stat_statements; PostgreSQL 13+. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.vector.insert.rows` | Sum | {vectors} | db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Requires pgvector and pg_stat_statements; PostgreSQL 13+. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.vector.search.calls` | Sum | {search} | postgresql.distance.function.name, db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Requires pgvector and pg_stat_statements; PostgreSQL 13+. Vector metrics are opt-in via receiver.metrics; distance classification depends on pgvector query syntax. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.vector.search.duration` | Sum | s | postgresql.distance.function.name, db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Requires pgvector and pg_stat_statements; PostgreSQL 13+. Vector metrics are opt-in via receiver.metrics; distance classification depends on pgvector query syntax. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.vector.search.rows_returned` | Sum | {rows} | postgresql.distance.function.name, db.namespace | Disabled by default; enable this metric in receiver.metrics. Requires pg_stat_statements. Requires pgvector and pg_stat_statements; PostgreSQL 13+. Vector metrics are opt-in via receiver.metrics; distance classification depends on pgvector query syntax. Metric-level semantic attributes are available with receiver.postgresql.useOTelSemconv; legacy mode uses per-entity resource attributes. |
| `postgresql.wal.age` | Gauge | s | - | Requires WAL and at least one replica. |
| `postgresql.wal.delay` | Gauge | s | operation, replication_client | Disabled by default; enable this metric in receiver.metrics. Requires WAL and at least one replica. Emitted only when receiver.postgresql.preciselagmetrics is enabled. |
| `postgresql.wal.lag` | Gauge | s | operation, replication_client | Requires WAL and at least one replica. Emitted only when receiver.postgresql.preciselagmetrics is disabled; the precise postgresql.wal.delay metric is emitted when the gate is enabled. |


## Logs

| Log/event name | Attributes | Notes |
|---|---|---|
| `db.server.query_sample` | db.system.name, db.namespace, db.query.text, user.name, postgresql.state, postgresql.pid, postgresql.application_name, network.peer.address, network.peer.port, postgresql.client_hostname, postgresql.query_start, postgresql.wait_event, postgresql.wait_event_type, postgresql.query_id, postgresql.total_exec_time, postgresql.blocking.pids, postgresql.blocking.start_time, postgresql.blocking.wait_duration, postgresql.blocking.lock.mode, postgresql.blocking.lock.type, postgresql.blocking.lock.relation, postgresql.blocking.transaction.start_time | Disabled by default; enable the event in receiver.logs. `postgresql.total_exec_time` is expressed in seconds. Query samples may carry trace/span IDs when application_name contains a valid W3C traceparent. |
| `db.server.top_query` | db.system.name, db.namespace, db.query.text, postgresql.calls, postgresql.rows, postgresql.shared_blks_dirtied, postgresql.shared_blks_hit, postgresql.shared_blks_read, postgresql.shared_blks_written, postgresql.temp_blks_read, postgresql.temp_blks_written, postgresql.queryid, postgresql.rolname, postgresql.total_exec_time, postgresql.total_plan_time, postgresql.query_plan | Disabled by default; enable the event in receiver.logs. `postgresql.total_exec_time` and `postgresql.total_plan_time` are expressed in seconds. |

## Sources

- receiver/postgresqlreceiver/metadata.yaml
- receiver/postgresqlreceiver/scraper.go
- receiver/postgresqlreceiver/client.go
- receiver/postgresqlreceiver/factory.go
- receiver/postgresqlreceiver/internal/metadata/generated_metrics.go
- receiver/postgresqlreceiver/internal/metadata/generated_logs.go
