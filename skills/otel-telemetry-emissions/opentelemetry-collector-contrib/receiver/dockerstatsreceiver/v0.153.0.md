---
repo: open-telemetry/opentelemetry-collector-contrib
path: receiver/dockerstatsreceiver
version: v0.153.0
scope_name: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/dockerstatsreceiver
commit_sha: 8c858ef12719865ba0a4b609e6adc12227b59c9a
last_verified: 2026-08-11
---

# Docker Stats Receiver

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `container.blockio.io_merged_recursive` | Sum (cumulative, monotonic) | `{operations}` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_merged_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.io_queued_recursive` | Sum (cumulative, monotonic) | `{operations}` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_queued_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.io_service_bytes_recursive` | Sum (cumulative, monotonic) | `By` | device_major, device_minor, operation | - |
| `container.blockio.io_service_time_recursive` | Sum (cumulative, monotonic) | `ns` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_service_time_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.io_serviced_recursive` | Sum (cumulative, monotonic) | `{operations}` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_serviced_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.io_time_recursive` | Sum (cumulative, monotonic) | `ms` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_time_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.io_wait_time_recursive` | Sum (cumulative, monotonic) | `ns` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.io_wait_time_recursive.enabled`. Only available with cgroups v1 |
| `container.blockio.sectors_recursive` | Sum (cumulative, monotonic) | `{sectors}` | device_major, device_minor, operation | Disabled by default; enable with `metrics.container.blockio.sectors_recursive.enabled`. Only available with cgroups v1 |
| `container.cpu.limit` | Gauge | `{cpus}` | - | Disabled by default; enable with `metrics.container.cpu.limit.enabled`. Only reported if the container has CPU limits set. |
| `container.cpu.logical.count` | Gauge | `{cpus}` | - | Disabled by default; enable with `metrics.container.cpu.logical.count.enabled`. |
| `container.cpu.shares` | Gauge | `1` | - | Disabled by default; enable with `metrics.container.cpu.shares.enabled`. |
| `container.cpu.throttling_data.periods` | Sum (cumulative, monotonic) | `{periods}` | - | Disabled by default; enable with `metrics.container.cpu.throttling_data.periods.enabled`. |
| `container.cpu.throttling_data.throttled_periods` | Sum (cumulative, monotonic) | `{periods}` | - | Disabled by default; enable with `metrics.container.cpu.throttling_data.throttled_periods.enabled`. |
| `container.cpu.throttling_data.throttled_time` | Sum (cumulative, monotonic) | `ns` | - | Disabled by default; enable with `metrics.container.cpu.throttling_data.throttled_time.enabled`. |
| `container.cpu.usage.kernelmode` | Sum (cumulative, monotonic) | `ns` | - | - |
| `container.cpu.usage.percpu` | Sum (cumulative, monotonic) | `ns` | core | Disabled by default; enable with `metrics.container.cpu.usage.percpu.enabled`. Only available with cgroups v1 |
| `container.cpu.usage.system` | Sum (cumulative, monotonic) | `ns` | - | Disabled by default; enable with `metrics.container.cpu.usage.system.enabled`. |
| `container.cpu.usage.total` | Sum (cumulative, monotonic) | `ns` | - | - |
| `container.cpu.usage.usermode` | Sum (cumulative, monotonic) | `ns` | - | - |
| `container.cpu.utilization` | Gauge | `1` | - | - |
| `container.memory.active_anon` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.active_anon.enabled`. |
| `container.memory.active_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.active_file.enabled`. |
| `container.memory.anon` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.anon.enabled`. Only available with cgroups v2 |
| `container.memory.cache` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.cache.enabled`. Only available with cgroups v1 |
| `container.memory.dirty` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.dirty.enabled`. Only available with cgroups v1 |
| `container.memory.fails` | Sum (cumulative, monotonic) | `{fails}` | - | Disabled by default; enable with `metrics.container.memory.fails.enabled`. |
| `container.memory.file` | Sum (cumulative, non-monotonic) | `By` | - | Only available with cgroups v2 |
| `container.memory.hierarchical_memory_limit` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.hierarchical_memory_limit.enabled`. Only available with cgroups v1 |
| `container.memory.hierarchical_memsw_limit` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.hierarchical_memsw_limit.enabled`. Only available with cgroups v1 |
| `container.memory.inactive_anon` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.inactive_anon.enabled`. |
| `container.memory.inactive_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.inactive_file.enabled`. |
| `container.memory.mapped_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.mapped_file.enabled`. Only available with cgroups v1 |
| `container.memory.percent` | Gauge | `1` | - | Not supported on Windows. |
| `container.memory.pgfault` | Sum (cumulative, monotonic) | `{faults}` | - | Disabled by default; enable with `metrics.container.memory.pgfault.enabled`. |
| `container.memory.pgmajfault` | Sum (cumulative, monotonic) | `{faults}` | - | Disabled by default; enable with `metrics.container.memory.pgmajfault.enabled`. |
| `container.memory.pgpgin` | Sum (cumulative, monotonic) | `{operations}` | - | Disabled by default; enable with `metrics.container.memory.pgpgin.enabled`. Only available with cgroups v1 |
| `container.memory.pgpgout` | Sum (cumulative, monotonic) | `{operations}` | - | Disabled by default; enable with `metrics.container.memory.pgpgout.enabled`. Only available with cgroups v1 |
| `container.memory.rss` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.rss.enabled`. Only available with cgroups v1 |
| `container.memory.rss_huge` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.rss_huge.enabled`. Only available with cgroups v1 |
| `container.memory.total_active_anon` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_active_anon.enabled`. Only available with cgroups v1 |
| `container.memory.total_active_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_active_file.enabled`. Only available with cgroups v1 |
| `container.memory.total_cache` | Sum (cumulative, non-monotonic) | `By` | - | Only available with cgroups v1 |
| `container.memory.total_dirty` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_dirty.enabled`. Only available with cgroups v1 |
| `container.memory.total_inactive_anon` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_inactive_anon.enabled`. Only available with cgroups v1 |
| `container.memory.total_inactive_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_inactive_file.enabled`. Only available with cgroups v1 |
| `container.memory.total_mapped_file` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_mapped_file.enabled`. Only available with cgroups v1 |
| `container.memory.total_pgfault` | Sum (cumulative, monotonic) | `{faults}` | - | Disabled by default; enable with `metrics.container.memory.total_pgfault.enabled`. Only available with cgroups v1 |
| `container.memory.total_pgmajfault` | Sum (cumulative, monotonic) | `{faults}` | - | Disabled by default; enable with `metrics.container.memory.total_pgmajfault.enabled`. Only available with cgroups v1 |
| `container.memory.total_pgpgin` | Sum (cumulative, monotonic) | `{operations}` | - | Disabled by default; enable with `metrics.container.memory.total_pgpgin.enabled`. Only available with cgroups v1 |
| `container.memory.total_pgpgout` | Sum (cumulative, monotonic) | `{operations}` | - | Disabled by default; enable with `metrics.container.memory.total_pgpgout.enabled`. Only available with cgroups v1 |
| `container.memory.total_rss` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_rss.enabled`. Only available with cgroups v1 |
| `container.memory.total_rss_huge` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_rss_huge.enabled`. Only available with cgroups v1 |
| `container.memory.total_unevictable` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_unevictable.enabled`. Only available with cgroups v1 |
| `container.memory.total_writeback` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.total_writeback.enabled`. Only available with cgroups v1 |
| `container.memory.unevictable` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.unevictable.enabled`. |
| `container.memory.usage.limit` | Sum (cumulative, non-monotonic) | `By` | - | - |
| `container.memory.usage.max` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.usage.max.enabled`. |
| `container.memory.usage.total` | Sum (cumulative, non-monotonic) | `By` | - | - |
| `container.memory.writeback` | Sum (cumulative, non-monotonic) | `By` | - | Disabled by default; enable with `metrics.container.memory.writeback.enabled`. Only available with cgroups v1 |
| `container.network.io.usage.rx_bytes` | Sum (cumulative, monotonic) | `By` | interface | - |
| `container.network.io.usage.rx_dropped` | Sum (cumulative, monotonic) | `{packets}` | interface | - |
| `container.network.io.usage.rx_errors` | Sum (cumulative, monotonic) | `{errors}` | interface | Disabled by default; enable with `metrics.container.network.io.usage.rx_errors.enabled`. Not supported on Windows. |
| `container.network.io.usage.rx_packets` | Sum (cumulative, monotonic) | `{packets}` | interface | Disabled by default; enable with `metrics.container.network.io.usage.rx_packets.enabled`. |
| `container.network.io.usage.tx_bytes` | Sum (cumulative, monotonic) | `By` | interface | - |
| `container.network.io.usage.tx_dropped` | Sum (cumulative, monotonic) | `{packets}` | interface | - |
| `container.network.io.usage.tx_errors` | Sum (cumulative, monotonic) | `{errors}` | interface | Disabled by default; enable with `metrics.container.network.io.usage.tx_errors.enabled`. Not supported on Windows. |
| `container.network.io.usage.tx_packets` | Sum (cumulative, monotonic) | `{packets}` | interface | Disabled by default; enable with `metrics.container.network.io.usage.tx_packets.enabled`. |
| `container.pids.count` | Sum (cumulative, non-monotonic) | `{pids}` | - | Disabled by default; enable with `metrics.container.pids.count.enabled`. Requires Docker API 1.23+ and a kernel with the pids cgroup. |
| `container.pids.limit` | Sum (cumulative, non-monotonic) | `{pids}` | - | Disabled by default; enable with `metrics.container.pids.limit.enabled`. Requires Docker API 1.23+ and a kernel with the pids cgroup. |
| `container.restarts` | Sum (cumulative, monotonic) | `{restarts}` | - | Disabled by default; enable with `metrics.container.restarts.enabled`. |
| `container.uptime` | Gauge | `s` | - | Disabled by default; enable with `metrics.container.uptime.enabled`. |

## Sources

- `receiver/dockerstatsreceiver/metadata.yaml`
- `receiver/dockerstatsreceiver/receiver.go`
- `receiver/dockerstatsreceiver/metric_helper.go`
- `receiver/dockerstatsreceiver/metric_helper_others.go`
- `receiver/dockerstatsreceiver/metric_helper_windows.go`
- `receiver/dockerstatsreceiver/factory.go`
- `receiver/dockerstatsreceiver/config.go`
- `receiver/dockerstatsreceiver/internal/metadata/generated_metrics.go`
