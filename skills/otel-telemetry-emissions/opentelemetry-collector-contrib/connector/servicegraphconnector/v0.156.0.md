---
repo: opentelemetry-collector-contrib
path: connector/servicegraphconnector
version: v0.156.0
scope_name: github.com/open-telemetry/opentelemetry-collector-contrib/connector/servicegraphconnector
commit_sha: 41e24cd516dd69a5b4277465cdb2ff4ef0676f49
last_verified: 2026-08-11
---

# Service Graph Connector

## Metrics

| Metric name | Type | Unit | Attributes | Notes |
|---|---|---|---|---|
| `otelcol_connector_servicegraph_dropped_spans` | Counter | 1 | - | - |
| `otelcol_connector_servicegraph_expired_edges` | Counter | 1 | - | - |
| `otelcol_connector_servicegraph_total_edges` | Counter | 1 | - | - |
| `traces_service_graph_request_total` | Sum (monotonic, cumulative) | - | `client`, `server`, `connection_type`, `failed`, configured `client_<dimension>` and `server_<dimension>`; optionally `virtual_node` | - |
| `traces_service_graph_request_failed_total` | Sum (monotonic, cumulative) | - | `client`, `server`, `connection_type`, `failed`, configured `client_<dimension>` and `server_<dimension>`; optionally `virtual_node` | - |
| `traces_service_graph_request_server` | Histogram or exponential histogram (cumulative) | s (or ms) | `client`, `server`, `connection_type`, `failed`, configured `client_<dimension>` and `server_<dimension>`; optionally `virtual_node` | Exponential histogram when `exponential_histogram_max_size` is configured; otherwise histogram using `latency_histogram_buckets`. Unit is ms when feature gate `connector.servicegraph.legacyLatencyUnitMs` is enabled. |
| `traces_service_graph_request_client` | Histogram or exponential histogram (cumulative) | s (or ms) | `client`, `server`, `connection_type`, `failed`, configured `client_<dimension>` and `server_<dimension>`; optionally `virtual_node` | Exponential histogram when `exponential_histogram_max_size` is configured; otherwise histogram using `latency_histogram_buckets`. Unit is ms when feature gate `connector.servicegraph.legacyLatencyUnitMs` is enabled. |
| `traces_service_graph_request_duration` | Histogram or exponential histogram (cumulative) | s (or ms) | `client`, `server`, `connection_type`, `failed`, configured `client_<dimension>` and `server_<dimension>`; optionally `virtual_node` | Replaces server and client latency metrics when feature gate `connector.servicegraph.legacyLatencyMetricNames` is enabled. Unit is ms when feature gate `connector.servicegraph.legacyLatencyUnitMs` is enabled. |

## Sources

- `connector/servicegraphconnector/connector.go`
- `connector/servicegraphconnector/factory.go`
- `connector/servicegraphconnector/config.go`
- `connector/servicegraphconnector/metadata.yaml`
- `connector/servicegraphconnector/internal/metadata/generated_telemetry.go`
